# 2. Создать текстовый файл (не программно), сохранить в нем несколько строк, выполнить подсчет
# количества строк, количества слов в каждой строке.
with open('text.txt') as f:
    lines = f.readlines()
    print(len(lines)) # считает количество строк
for line in lines:
    words = line.split()
    print(len(words)) # считает количество слов в каждой строке